import 'package:flutter/material.dart';

// The main function is the starting point of the all our flutter projects.
// for indentation we can use {} in the place of =>
void main() => runApp(
      MaterialApp(
        home: Center(
          child: Text("Hello World"),
        ),
      ),
    );
